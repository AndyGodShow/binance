# Optional runtime dependencies for richer exports.
# The pipeline runs with the Python standard library; install these to enable YAML and parquet workflows.
pandas>=2.2
pyarrow>=15
PyYAML>=6
numpy>=1.26
